name: keepalive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS -o /dev/null -w "%{http_code}\n" --retry 3 --max-time 30 "${{ vars.APP_URL }}"
